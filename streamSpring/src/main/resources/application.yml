spring:
  servlet:
    multipart:
      max-file-size: 100000MB
      max-request-size: 10000000MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root123@
    url: jdbc:mysql://localhost:3306/stream_spring
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  servlet:
    context-path: /stream_spring

